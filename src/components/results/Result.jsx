import { Grid } from "@material-ui/core";
import UserCard from "../../shared/components/Card";

const Result = () => {
  return (
    <Grid container>
      <UserCard />
    </Grid>
  );
};

export default Result;
